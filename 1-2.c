#include <stdio.h>

main()
{
	printf("hello world \n"); 
	/* \a has no effect, c, d, g, h, i, j, k unrecognized 
	/* r erases a tab? */
	/* e deletes the next character */
	/* f inserts a newline and whitespace to old position */
	printf("hello world will be erased \H and some more stuff\n"); 
	printf("hello world again \n"); 
}
